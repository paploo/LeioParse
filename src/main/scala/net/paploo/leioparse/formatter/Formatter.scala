package net.paploo.leioparse.formatter

import java.io.{BufferedWriter, OutputStream, OutputStreamWriter, PrintWriter, Writer}

import net.paploo.leioparse.util.extensions.Implicits._

//trait Formatter extends (OutputStream => OutputStream) {
//  def format(out: OutputStream): Unit
//
//  override def apply(out: OutputStream): OutputStream = format(out)
//}
